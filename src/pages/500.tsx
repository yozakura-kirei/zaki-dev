import PageWrapper from '@/components/templates/PageWrapper';

export default function Custom500() {
  return (
    <PageWrapper isGrid={false}>
      <h1>サーバー側でエラーが発生しています</h1>
    </PageWrapper>
  );
}
